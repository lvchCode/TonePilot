<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tonepilot.infrastructure.shared.persistence.mapper.DomainSnapshotMapper">

    <update id="updateSnapshot" parameterType="com.tonepilot.repository.shared.DomainSnapshotRecord">
        UPDATE domain_snapshot
        SET payload_json = #{payloadJson}, updated_at = #{updatedAt}
        WHERE domain_type = #{domainType} AND domain_id = #{domainId}
    </update>

    <insert id="insertSnapshot" parameterType="com.tonepilot.repository.shared.DomainSnapshotRecord">
        INSERT INTO domain_snapshot (
            domain_type, domain_id, payload_json, created_at, updated_at
        ) VALUES (
            #{domainType}, #{domainId}, #{payloadJson}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="findPayload" parameterType="com.tonepilot.repository.shared.DomainSnapshotKey" resultType="string">
        SELECT payload_json
        FROM domain_snapshot
        WHERE domain_type = #{domainType} AND domain_id = #{domainId}
    </select>

    <select id="listPayloads" parameterType="string" resultType="string">
        SELECT payload_json
        FROM domain_snapshot
        WHERE domain_type = #{_parameter}
    </select>
</mapper>
